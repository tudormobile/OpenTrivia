<UserControl x:Class="Tudormobile.OpenTrivia.UI.Views.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tudormobile.OpenTrivia.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ScrollViewer>
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Game View (not completed)" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24"/>
                <local:CategoriesView DataContext="{Binding Categories}"/>
                <ListBox ItemsSource="{Binding Questions}" ItemTemplate="{StaticResource OpenTrivia.UI.QuestionWithAnswersTemplate}"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
